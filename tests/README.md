This directory contains repository-level tests that do not belong to `apps/web`.

- `test_arduino_auto_mouse.py` validates that the Arduino ultrasonic mouse sketch and its documentation stay aligned on pin mapping and default detection range.
